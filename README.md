# 🔐 Secrets & Revelations Tracker

Расширение для **SillyTavern** — отслеживает секреты, тайны и раскрытия между `{{user}}` и `{{char}}` в RP-чатах.

---

## Возможности

- **Плавающий виджет** 🔐 — счётчик раскрытых / скрытых секретов, перетаскивается по экрану
- **Боковая панель** — добавление, удаление и редактирование секретов по категориям
- **Инъекция в промпт** — трекер незаметно передаётся модели через `setExtensionPrompt()`, не засоряя чат-лог
- **🔍 Сканирование чата** — AI сам анализирует историю переписки и карточку персонажа, находит секреты и добавляет их без дублей
- **⚡ Авто-детект раскрытий** — после каждого ответа `{{char}}` расширение ловит маркер `[REVEAL: ...]` и автоматически переводит секрет в «известен»
- **Свой API** — подключи любой OpenAI-совместимый эндпоинт (OpenAI, Mistral, LM Studio, Ollama и др.), список моделей загружается кнопкой 🔄
- **Привязка к чату** — данные хранятся отдельно для каждого чата и персонажа, не смешиваются
- **🐛 Дебаг** — кнопка показывает полный дамп: что уходит в промпт, что читается из карточки, какая история чата анализируется

---

## Категории секретов

| Иконка | Тег | Значение |
|--------|-----|----------|
| 💣 | `dangerous` | Угроза жизни / серьёзный вред |
| 💔 | `personal` | Личное / эмоциональное |
| 🗡️ | `kompromat` | Рычаг давления |
| — | `none` | Без категории |

---

## Установка

**Локально:**
```
public/scripts/extensions/third-party/SRT/
```
или
```
data/<твой-профиль>/extensions/SRT/
```

**Через GitHub:** Extensions → Install Extension → вставь URL репозитория.

---

## Быстрый старт

1. Открой чат с персонажем
2. Нажми 🔐 → откроется панель
3. Нажми **🔍 Сканировать чат** — AI найдёт секреты сам
4. Или добавь вручную в нужную секцию
5. Чекбокс 🔓 = секрет стал известен другой стороне

---

## Свой API

В настройках расширения → **API для сканирования**:
- **Endpoint** — вставь как есть, нормализуется автоматически (`/v1`, `/v1/chat/completions` — всё одно)
- **API Key** — ключ доступа
- **Модель** — нажми 🔄 для загрузки списка, выбери нужную

Если оставить пустым — используется встроенный генератор SillyTavern.

---

## Данные

Хранятся в `chatMetadata` под ключом `srt_state_v1__<charId>__<chatId>`.  
Каждый чат изолирован — секреты одного персонажа не попадают к другому.

